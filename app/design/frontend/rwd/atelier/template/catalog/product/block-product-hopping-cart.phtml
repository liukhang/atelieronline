<?php
/**
 * Magento
 *
 * NOTICE OF LICENSE
 *
 * This source file is subject to the Academic Free License (AFL 3.0)
 * that is bundled with this package in the file LICENSE_AFL.txt.
 * It is also available through the world-wide-web at this URL:
 * http://opensource.org/licenses/afl-3.0.php
 * If you did not receive a copy of the license and are unable to
 * obtain it through the world-wide-web, please send an email
 * to license@magento.com so we can send you a copy immediately.
 *
 * DISCLAIMER
 *
 * Do not edit or add to this file if you wish to upgrade Magento to newer
 * versions in the future. If you wish to customize Magento for your
 * needs please refer to http://www.magento.com for more information.
 *
 * @category    design
 * @package     rwd_default
 * @copyright   Copyright (c) 2006-2017 X.commerce, Inc. and affiliates (http://www.magento.com)
 * @license     http://opensource.org/licenses/afl-3.0.php  Academic Free License (AFL 3.0)
 */
?>
<?php
/**
 * Product list template
 *
 * @see Mage_Catalog_Block_Product_List
 */
/* @var $this Mage_Catalog_Block_Product_List */
?>
<?php
    $_productCollections=$this->getLoadedProductCollection();
	
	$cart = Mage::getModel('checkout/cart')->getQuote();
	foreach ($cart->getAllItems() as $item) {
		$firstItemId = $item->getProductId();
		break;
	}
    $_helper = $this->helper('catalog/output');
	
	
	$_products = Mage::getModel('catalog/product')->load($firstItemId);
	$categoryIds = $_products->getCategoryIds();
	$category_id  = $categoryIds[count($categoryIds)-1];
	
	
	$array1 = array();
	$array1[] = 3;
	$subcategoryCollections1 = Mage::getModel('catalog/category')
		->getCollection()
		->addFieldToFilter('path', ['like' => Mage::getModel('catalog/category')->load(3)->getData('path') . '/%']);
	foreach($subcategoryCollections1 as $subcategoryCollection1){
		$array1[] = $subcategoryCollection1->getId();
	}
	if(in_array($category_id, $array1)){
		$new_category_id = 28;
	}


//array2

$array2 = array();
$array2[] = 13;
$subcategoryCollections2 = Mage::getModel('catalog/category')
    ->getCollection()
    ->addFieldToFilter('path', ['like' => Mage::getModel('catalog/category')->load(13)->getData('path') . '/%']);
foreach($subcategoryCollections2 as $subcategoryCollection2){
	$array2[] = $subcategoryCollection2->getId();
}
if(in_array($category_id, $array2)){
	$new_category_id = 3;
}

//array3

$array3 = array();
$array3[] = 17;
$subcategoryCollections3 = Mage::getModel('catalog/category')
    ->getCollection()
    ->addFieldToFilter('path', ['like' => Mage::getModel('catalog/category')->load(17)->getData('path') . '/%']);
foreach($subcategoryCollections3 as $subcategoryCollection3){
	$array3[] = $subcategoryCollection3->getId();
}
if(in_array($category_id, $array3)){
	$new_category_id = 3;
}

//array4

$array4 = array();
$array4[] = 24;
$subcategoryCollections4 = Mage::getModel('catalog/category')
    ->getCollection()
    ->addFieldToFilter('path', ['like' => Mage::getModel('catalog/category')->load(24)->getData('path') . '/%']);
foreach($subcategoryCollections4 as $subcategoryCollection4){
	$array4[] = $subcategoryCollection4->getId();
}
if(in_array($category_id, $array4)){
	$new_category_id = 3;
}

//array5

$array5 = array();
$array5[] = 28;
$subcategoryCollections5 = Mage::getModel('catalog/category')
    ->getCollection()
    ->addFieldToFilter('path', ['like' => Mage::getModel('catalog/category')->load(28)->getData('path') . '/%']);
foreach($subcategoryCollections5 as $subcategoryCollection5){
	$array5[] = $subcategoryCollection5->getId();
}
if(in_array($category_id, $array5)){
	$new_category_id = 3;
}

//array6

$array6 = array();
$array6[] = 61;
$subcategoryCollections6 = Mage::getModel('catalog/category')
    ->getCollection()
    ->addFieldToFilter('path', ['like' => Mage::getModel('catalog/category')->load(61)->getData('path') . '/%']);
foreach($subcategoryCollections6 as $subcategoryCollection6){
	$array6[] = $subcategoryCollection6->getId();
}
if(in_array($category_id, $array6)){
	$new_category_id = 3;
}

//array7

$array7 = array();
$array7[] = 32;
$subcategoryCollections7 = Mage::getModel('catalog/category')
    ->getCollection()
    ->addFieldToFilter('path', ['like' => Mage::getModel('catalog/category')->load(32)->getData('path') . '/%']);
foreach($subcategoryCollections7 as $subcategoryCollection7){
	$array7[] = $subcategoryCollection7->getId();
}
if(in_array($category_id, $array7)){
	$new_category_id = 28;
}

//array8

$array8 = array();
$array8[] = 53;
$subcategoryCollections8 = Mage::getModel('catalog/category')
    ->getCollection()
    ->addFieldToFilter('path', ['like' => Mage::getModel('catalog/category')->load(53)->getData('path') . '/%']);
foreach($subcategoryCollections8 as $subcategoryCollection8){
	$array8[] = $subcategoryCollection8->getId();
}
if(in_array($category_id, $array8)){
	$new_category_id = 3;
}


	
$category = Mage::getModel('catalog/category')->load($new_category_id);
$_productCollection = Mage::getResourceModel('catalog/product_collection')
                                 ->addCategoryFilter($category)
                                 ->addAttributeToFilter('status',1)
                                 ->addAttributeToFilter('visibility',4)
								 ->setPageSize(4)
								->setCurPage(1);
?>
	
<?php if(!$_productCollection->count()): ?>
<p class="note-msg"><?php echo $this->__('There are no products matching the selection.') ?></p>
<?php else: ?>
<div class="product-slick">
    <h2><?php echo __('Potrebbero interessarti anche') ?></h2>
    <?php $_collectionSize = $_productCollection->count() ?>
    <?php $_columnCount = $this->getColumnCount(); ?>
    <ul id="product-slick-slide" class="product-slick-slide products-grid products-grid--max-<?php echo $_columnCount; ?>-col">
        <?php $i=0; foreach ($_productCollection as $_product): ?>
		<?php $_product = Mage::getModel('catalog/product')->load($_product->getId()); ?>
            <?php $i++; ?>
            <?php /*if ($i++%$_columnCount==0): ?>
            <?php endif*/ ?>
            <li class="item<?php if(($i-1)%$_columnCount==0): ?> first<?php elseif($i%$_columnCount==0): ?> last<?php endif; ?>">
                <a href="<?php echo $_product->getProductUrl() ?>" title="<?php echo $this->stripTags($this->getImageLabel($_product, 'small_image'), null, true) ?>" class="product-image">
                    <?php $_imgSize = 258; ?>
                    <div class="img-hover">
                        <img id="product-collection-image-<?php echo $_product->getId(); ?>"
                         src="<?php echo $this->helper('catalog/image')->init($_product, 'small_image')->resize(258,315); ?>"
                         alt="<?php echo $this->stripTags($this->getImageLabel($_product, 'small_image'), null, true) ?>" />
                         <span class="vedi-pr"><?php echo __('Vedi prodotto') ?></span>  
                    </div>
                </a>
                <div class="product-info">
                    <h2 class="product-name"><a href="<?php echo $_product->getProductUrl() ?>" title="<?php echo $this->stripTags($_product->getName(), null, true) ?>"><?php echo $_helper->productAttribute($_product, $_product->getName(), 'name') ?></a></h2>
                    <?php echo $this->getPriceHtml($_product, true) ?>
                </div>
            </li>
            <?php if($i==8){ break; } ?>
            <?php /*if ($i%$_columnCount==0 || $i==$_collectionSize): ?>
            <?php endif*/ ?>
        <?php endforeach ?>
    </ul>
    <script type="text/javascript">decorateGeneric($$('ul.products-grid'), ['odd','even','first','last'])</script>
</div>


<div class="product-slick-mobie">
    <h2><?php echo __('Potrebbero interessarti anche') ?></h2>
    <?php $_collectionSize = $_productCollection->count() ?>
    <?php $_columnCount = $this->getColumnCount(); ?>
    <ul id="product-slick-slide-mobie" class="owl-carousel products-grid products-grid--max-<?php echo $_columnCount; ?>-col">
        <?php $i=0; foreach ($_productCollection as $_product): ?>
            <?php $i++; ?>
            <?php /*if ($i++%$_columnCount==0): ?>
            <?php endif*/ ?>
            <li class="item<?php if(($i-1)%$_columnCount==0): ?> first<?php elseif($i%$_columnCount==0): ?> last<?php endif; ?>">
                <a href="<?php echo $_product->getProductUrl() ?>" title="<?php echo $this->stripTags($this->getImageLabel($_product, 'small_image'), null, true) ?>" class="product-image">
                    <?php $_imgSize = 258; ?>
                    <div class="img-hover">
                        <img id="product-collection-image-<?php echo $_product->getId(); ?>"
                         src="<?php echo $this->helper('catalog/image')->init($_product, 'small_image')->resize(258,315); ?>"
                         alt="<?php echo $this->stripTags($this->getImageLabel($_product, 'small_image'), null, true) ?>" />
                         <span class="vedi-pr"><?php echo __('Vedi prodotto') ?></span>  
                    </div>
                </a>
                <div class="product-info">
                    <h2 class="product-name"><a href="<?php echo $_product->getProductUrl() ?>" title="<?php echo $this->stripTags($_product->getName(), null, true) ?>"><?php echo $_helper->productAttribute($_product, $_product->getName(), 'name') ?></a></h2>
                    <?php echo $this->getPriceHtml($_product, true) ?>
                </div>
            </li>
            <?php if($i==8){ break; } ?>
            <?php /*if ($i%$_columnCount==0 || $i==$_collectionSize): ?>
            <?php endif*/ ?>
        <?php endforeach ?>
    </ul>
    <script type="text/javascript">decorateGeneric($$('ul.products-grid'), ['odd','even','first','last'])</script>
</div>

<?php endif; ?>
<?php
// Provides a block where additional page components may be attached, primarily good for in-page JavaScript
if ($this->getChild('after')) {
    $_afterChildren = $this->getChild('after')->getSortedChildren();
    foreach ($_afterChildren as $_afterChildName) {
        $_afterChild = $this->getChild('after')->getChild($_afterChildName);
        //set product collection on after blocks
        $_afterChild->setProductCollection($_productCollection);
        echo $_afterChild->toHtml();
    }
}
?>


<script>
    jQuery('.product-slick-slide').slick({
        dots: false,
        vertical: true,
        slidesToShow: 2,
        slidesToScroll: 2,
        verticalSwiping: true,
        nextArrow: '<i class="fas fa-chevron-down"></i>',
        prevArrow: '<i class="fas fa-chevron-up"></i>'
    });
</script>

<script type="text/javascript">
    jQuery('#product-slick-slide-mobie.owl-carousel').owlCarousel({
        loop: false,
        navSpeed: 1000,
        smartSpeed:1000,
        margin: 40,
        autoplay:false,
        nav:true,
        navText:['<a href="javascript:void(0)"><i class="fas fa-angle-left"></i></a>','<a href="javascript:void(0)"><i class="fas fa-angle-right"></i></a>'],
        responsiveClass: false,
        responsive: {
            0: {
                items: 1,
                // autoplay:true,
                nav: true
            },
            361: {
                items: 2,
                // autoplay:true,
                nav: true
            },
            641: {
                items: 2,
                // autoplay:true,
                nav: true
            },
            768: {
                items: 3,
                // nav: true,
                loop: false,
                // autoplay:true,
                margin: 20
            },
            1024: {
                items: 4,
                nav: true,
                loop: true,
                // autoplay:true,
                dots: false,
                margin: 20
            }               
        }
    });
</script>